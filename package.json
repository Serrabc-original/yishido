{
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/**/*.test.js",
    "test:inputs": "node scripts/test-all-input-cases.js"
  }
}
